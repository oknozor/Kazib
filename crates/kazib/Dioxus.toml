[application]

[web.app]

# HTML title tag content
title = "kazib"

# include `assets` in web platform
[web.resource]

# Additional CSS style files
style = []

# Additional JavaScript files
script = []

[web.resource.dev]

# Javascript code file
# serve: [dev-server] only
script = []

[bundle]
# Skip wasm-opt to avoid SIGABRT crashes in cross-platform Docker builds
skip_wasm_opt = true
